---
layout: base
title: Texte
description: "Gedichte, Essays und persönliche Texte von Jochen Hornung über Menschlichkeit, Tiefe und das Fühlen."
---
<style>
  .content-page {
    padding-top: 1rem;
  }

  .category-tabs {
    display: flex;
    gap: 2rem;
    margin-bottom: 3rem;
    border-bottom: 1px solid #eee;
    padding-bottom: 1rem;
  }
  
  .category-tab {
    background: none;
    border: none;
    font-size: 1.1rem;
    color: #666;
    cursor: pointer;
    padding: 0.5rem 0;
    font-family: inherit;
  }
  
  .category-tab.active {
    color: #1a1a1a;
    border-bottom: 2px solid #1a1a1a;
    margin-bottom: -1.1rem;
  }
  
  .category-content {
    display: none;
  }
  
  .category-content.active {
    display: block;
  }
  
  .text-item h3 a {
    color: #1a1a1a;
    text-decoration: none;
  }
  
  .text-item h3 a:hover {
    text-decoration: underline;
  }
  
  .coming-soon {
    color: #999;
    font-style: italic;
    margin-top: 2rem;
  }
</style>

<div class="content-page">
    <a href="/" class="back-link">← Zurück</a>

    <div class="text-content">
        {% if collections.sortedTexte.length > 0 %}
        
        <div class="category-tabs">
            <button class="category-tab active" onclick="showCategory('gedichte')">Gedichte</button>
            <button class="category-tab" onclick="showCategory('essays')">Essays</button>
        </div>

        <div id="gedichte" class="category-content active">
            <ul class="text-list">
            {% for text in collections.sortedTexte %}
                {% if text.data.type == "Gedicht" %}
                <li class="text-item">
                    <h3><a href="{{ text.url }}">{{ text.data.title }}</a></h3>
                    <div class="text-meta">
                        {{ text.date | readableDate }}
                    </div>
                    {% if text.data.excerpt %}
                    <div class="text-excerpt">
                        {{ text.data.excerpt }}
                    </div>
                    {% endif %}
                </li>
                {% endif %}
            {% endfor %}
            </ul>
        </div>

        <div id="essays" class="category-content">
            {% set hasEssays = false %}
            {% for text in collections.sortedTexte %}
                {% if text.data.type == "Essay" %}
                    {% set hasEssays = true %}
                {% endif %}
            {% endfor %}
            
            {% if hasEssays %}
            <ul class="text-list">
            {% for text in collections.sortedTexte %}
                {% if text.data.type == "Essay" %}
                <li class="text-item">
                    <h3><a href="{{ text.url }}">{{ text.data.title }}</a></h3>
                    <div class="text-meta">
                        {{ text.date | readableDate }}
                    </div>
                    {% if text.data.excerpt %}
                    <div class="text-excerpt">
                        {{ text.data.excerpt }}
                    </div>
                    {% endif %}
                </li>
                {% endif %}
            {% endfor %}
            </ul>
            {% else %}
            <p class="coming-soon">Kommt bald ...</p>
            {% endif %}
        </div>

        {% else %}
            <p>Hier entstehen Gedichte und Essays über Menschlichkeit, Tiefe und das Leben zwischen den Zeilen.</p>
            <p><em>Bald verfügbar.</em></p>
        {% endif %}
    </div>
</div>

<script>
function showCategory(category) {
    document.querySelectorAll('.category-content').forEach(el => el.classList.remove('active'));
    document.querySelectorAll('.category-tab').forEach(el => el.classList.remove('active'));
    
    document.getElementById(category).classList.add('active');
    event.target.classList.add('active');
}
</script>
