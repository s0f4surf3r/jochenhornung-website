---
layout: base
title: Texte
---
<div class="content-page">
    <a href="/" class="back-link">← Zurück</a>

    <h1 class="page-title">Texte</h1>

    <div class="text-content">
        {% if collections.sortedTexte.length > 0 %}
        
        <section class="text-section">
            <h2 class="section-title">Gedichte</h2>
            <ul class="text-list">
            {% for text in collections.sortedTexte %}
                {% if text.data.type == "Gedicht" %}
                <li class="text-item">
                    <h3><a href="{{ text.url }}">{{ text.data.title }}</a></h3>
                    <div class="text-meta">
                        {{ text.date | readableDate }}
                    </div>
                    {% if text.data.excerpt %}
                    <div class="text-excerpt">
                        {{ text.data.excerpt }}
                    </div>
                    {% endif %}
                </li>
                {% endif %}
            {% endfor %}
            </ul>
        </section>

        <section class="text-section">
            <h2 class="section-title">Essays</h2>
            <ul class="text-list">
            {% for text in collections.sortedTexte %}
                {% if text.data.type == "Essay" %}
                <li class="text-item">
                    <h3><a href="{{ text.url }}">{{ text.data.title }}</a></h3>
                    <div class="text-meta">
                        {{ text.date | readableDate }}
                    </div>
                    {% if text.data.excerpt %}
                    <div class="text-excerpt">
                        {{ text.data.excerpt }}
                    </div>
                    {% endif %}
                </li>
                {% endif %}
            {% endfor %}
            </ul>
        </section>

        {% else %}
            <p>Hier entstehen Gedichte und Essays über Menschlichkeit, Tiefe und das Leben zwischen den Zeilen.</p>
            <p><em>Bald verfügbar.</em></p>
        {% endif %}
    </div>
</div>
