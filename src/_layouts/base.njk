<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>{% if title %}{{ title }} - {% endif %}Jochen Hornung</title>

    <meta name="description" content="Raum für Menschlichkeit und Tiefe. Coaching, Texte und Gedanken von Jochen Hornung aus Berlin.">
    <meta name="author" content="Jochen Hornung">

    <meta property="og:type" content="website">
    <meta property="og:url" content="https://jochenhornung.de{{ page.url }}">
    <meta property="og:title" content="{% if title %}{{ title }} - {% endif %}Jochen Hornung">
    <meta property="og:description" content="Raum für Menschlichkeit und Tiefe. Coaching, Texte und Gedanken von Jochen Hornung aus Berlin.">
    <meta property="og:image" content="https://jochenhornung.de/images/hero_for_SM_WA.jpg">

    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    <link rel="apple-touch-icon" sizes="180x180" href="/images/apple-touch-icon.png">

    <link rel="stylesheet" href="/css/style.css">
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>

    <style>
        /* Layout-Logik für Header und Footer */
        .site-header {
            padding: calc(3rem + env(safe-area-inset-top)) 2rem 1rem;
            max-width: 800px;
            margin: 0 auto;
        }

        .site-header h1 { font-size: 1.8rem; font-weight: 400; margin-bottom: 0.3rem; }
        .site-header h1 a { color: #1a1a1a; text-decoration: none; }

        .site-tagline {
            font-size: 1rem;
            color: #666;
            margin-bottom: 1rem;
            font-family: Georgia, serif;
            font-style: italic;
        }

        .site-nav { display: flex; gap: 0.5rem; flex-wrap: wrap; }
        .site-nav a { color: #1a1a1a; text-decoration: underline; }
        .site-nav a.active { color: #999; text-decoration: none; }
        .site-nav span { color: #999; }

        .site-footer {
            text-align: center;
            padding: 2rem 0;
            padding-bottom: calc(2rem + env(safe-area-inset-bottom));
            border-top: 1px solid #eee;
            font-size: 0.9rem;
            color: #666;
            background: #ffffff;
        }

        .site-footer a { color: #666; }

        /* Content Page Styles */
        .content-page { padding-top: 1rem; }
        .content-page .text-content { font-family: Georgia, serif; }
    </style>
</head>
<body>
    {% if page.url != "/" %}
    <header class="site-header">
        <h1><a href="/">Jochen Hornung</a></h1>
        <p class="site-tagline">Raum für Menschlichkeit und Tiefe</p>
        <nav class="site-nav">
            <a href="/texte/"{% if page.url.includes("/texte") %} class="active"{% endif %}>Texte</a>
            <span>/</span>
            <a href="/coaching/"{% if page.url.includes("/coaching") %} class="active"{% endif %}>Coaching</a>
            <span>/</span>
            <a href="/werkzeuge/"{% if page.url.includes("/werkzeuge") %} class="active"{% endif %}>Werkzeuge</a>
            <span>/</span>
            <a href="/kaktus/"{% if page.url.includes("/kaktus") %} class="active"{% endif %}>Kaktus</a>
            <span>/</span>
            <a href="/ueber/"{% if page.url.includes("/ueber") %} class="active"{% endif %}>Über mich</a>
        </nav>
    </header>
    {% endif %}

    {{ content | safe }}

    <footer class="site-footer">
        © 2026 Jochen Hornung | <a href="/impressum">Impressum</a> | <a href="/datenschutz">Datenschutz</a>
    </footer>
</body>
</html>
