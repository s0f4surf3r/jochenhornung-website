---
layout: base.njk
title: Raum für Menschlichkeit und Tiefe
description: "Texte über das Menschsein, Coaching, digitale Werkzeuge — von Jochen Hornung, Coach und Entwickler aus Berlin."
heroImage: "/images/og-image.png"
---

<style>
/* ══════════════════════════════════════
   SAND-WELT — Hell, warm, einladend.
   Kontrast-Achse: 35% (weich, nicht laut)
   Noise-Grain für analoge Wärme.
   ══════════════════════════════════════ */

:root {
  --sand-bg: #f5f0e6;
  --sand-surface: #ffffff;
  --sand-text: #1a1816;
  --sand-text-muted: #6b6054;
  --sand-text-dim: #9a8e7e;
  --sand-accent: #b8942a;
  --sand-accent-hover: #a07e1e;
  --sand-border: #e0d8cc;
  --sand-warm: #ede4d2;
  --sand-card: #faf8f4;
}

html { scroll-behavior: smooth; }

@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  .reveal { opacity: 1 !important; transform: none !important; }
}

body {
  background-color: var(--sand-bg);
  color: var(--sand-text);
  -webkit-tap-highlight-color: transparent;
  overflow-x: hidden;
  position: relative;
}

/* Noise-Grain Overlay — analoges Papier */
body::before {
  content: '';
  position: fixed;
  inset: 0;
  background: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  opacity: 0.035;
  pointer-events: none;
  z-index: 0;
}

/* ── HERO ── */
.hero-home {
  position: relative;
  z-index: 1;
}

.hero-image {
  width: 100%;
  display: block;
  border-bottom: 1px solid var(--sand-border);
}

.hero-image img {
  width: 100%;
  height: auto;
  display: block;
}

.hero-body {
  max-width: 1100px;
  margin: 0 auto;
  padding: 4rem 4rem 3rem;
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  gap: 3rem;
}

.hero-text {
  flex: 1;
}

.hero-name {
  font-family: Georgia, 'Times New Roman', serif;
  font-size: clamp(1.8rem, 3.5vw, 2.6rem);
  font-weight: 400;
  line-height: 1.4;
  color: var(--sand-text);
  margin: 0;
  max-width: 500px;
}

/* hero-tagline removed — h1 IS the tagline now */

.hero-nav {
  display: flex;
  gap: 1.3rem;
  flex-wrap: wrap;
  align-items: center;
  font-family: -apple-system, 'Helvetica Neue', sans-serif;
  font-size: 0.95rem;
  max-width: 1100px;
  margin: 0 auto;
  padding: 1.5rem 4rem;
}

.hero-nav .nav-home {
  font-weight: 600;
  color: var(--sand-text);
  margin-right: auto;
  display: inline-flex;
  align-items: center;
  gap: 0.6rem;
}

.nav-logo {
  width: 32px;
  height: 32px;
  border-radius: 50%;
}

.hero-nav a {
  color: var(--sand-text-muted);
  text-decoration: none;
  padding: 0.5rem 0;
  min-height: 44px;
  display: inline-flex;
  align-items: center;
  transition: color 0.2s;
}

@media (hover: hover) {
  .hero-nav a:hover { color: var(--sand-text); }
}

.hero-nav a:active { color: var(--sand-accent); }

.hero-nav .nav-sep {
  color: var(--sand-border);
  user-select: none;
}

/* ── SECTIONS ── */
.section {
  position: relative;
  z-index: 1;
  padding: 5rem 4rem;
  max-width: 1100px;
  margin: 0 auto;
}

.section-label {
  font-family: -apple-system, 'Helvetica Neue', sans-serif;
  font-size: 0.8rem;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--sand-text-dim);
  margin: 0 0 1.5rem 0;
}

.section-heading {
  font-family: Georgia, 'Times New Roman', serif;
  font-size: clamp(1.6rem, 3vw, 2.2rem);
  font-weight: 400;
  line-height: 1.4;
  color: var(--sand-text);
  margin: 0 0 2rem 0;
}

.section-text {
  font-family: Georgia, 'Times New Roman', serif;
  font-size: 1.08rem;
  line-height: 1.75;
  color: var(--sand-text-muted);
  max-width: 600px;
  margin-bottom: 3rem;
}

/* ── CARDS ── */
.card-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem;
}

.card {
  background: var(--sand-card);
  border: 1px solid var(--sand-border);
  border-radius: 10px;
  padding: 2rem 1.8rem;
  text-decoration: none;
  color: var(--sand-text);
  transition: border-color 0.25s, transform 0.25s;
  display: block;
  min-height: 44px;
}

@media (hover: hover) {
  .card:hover {
    border-color: var(--sand-accent);
    transform: translateY(-2px);
  }
}

.card:active {
  border-color: var(--sand-accent);
  transform: scale(0.98);
}

.card-icon {
  margin-bottom: 1rem;
  display: block;
  color: var(--sand-text-dim);
}

.card-title {
  font-family: -apple-system, 'Helvetica Neue', sans-serif;
  font-size: 1.05rem;
  font-weight: 600;
  margin: 0 0 0.6rem 0;
}

.card-desc {
  font-family: Georgia, 'Times New Roman', serif;
  font-size: 0.95rem;
  line-height: 1.65;
  color: var(--sand-text-muted);
  margin: 0;
}

/* ── TEXTE ── */
.texte-teaser {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
  align-items: start;
}

.texte-quote {
  font-family: Georgia, 'Times New Roman', serif;
  font-size: clamp(1.2rem, 2.5vw, 1.5rem);
  line-height: 1.65;
  color: var(--sand-text);
  font-style: italic;
  border-left: 2px solid var(--sand-accent);
  padding-left: 1.5rem;
  margin: 0;
}

.texte-link-wrap {
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.texte-link-wrap p {
  font-family: Georgia, 'Times New Roman', serif;
  font-size: 1.05rem;
  line-height: 1.75;
  color: var(--sand-text-muted);
  margin: 0 0 1.5rem 0;
}

/* ── STREAMING ── */
.streaming-teaser {
  background: var(--sand-card);
  border: 1px solid var(--sand-border);
  border-radius: 12px;
  padding: 2.5rem;
  display: flex;
  align-items: center;
  gap: 2rem;
  text-decoration: none;
  color: var(--sand-text);
  transition: border-color 0.25s;
}

@media (hover: hover) {
  .streaming-teaser:hover { border-color: var(--sand-accent); }
}

.streaming-teaser:active { border-color: var(--sand-accent); }

.streaming-avatar {
  width: 72px;
  height: 72px;
  border-radius: 50%;
  flex-shrink: 0;
}

.streaming-info p {
  font-family: Georgia, 'Times New Roman', serif;
  font-size: 1.05rem;
  line-height: 1.65;
  color: var(--sand-text-muted);
  margin: 0.5rem 0 0 0;
}

.streaming-badge {
  display: inline-block;
  font-family: -apple-system, 'Helvetica Neue', sans-serif;
  font-size: 0.75rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--sand-accent);
  border: 1px solid var(--sand-accent);
  border-radius: 4px;
  padding: 0.25rem 0.6rem;
}

/* ── LINKS ── */
.text-link {
  color: var(--sand-text);
  text-decoration: none;
  border-bottom: 1px solid var(--sand-accent);
  padding-bottom: 1px;
  transition: border-color 0.2s;
  font-family: Georgia, 'Times New Roman', serif;
  font-size: 1.05rem;
}

@media (hover: hover) {
  .text-link:hover { border-color: var(--sand-text); }
}

/* ── DIVIDER ── */
.section-divider {
  position: relative;
  z-index: 1;
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 4rem;
}

.section-divider hr {
  border: none;
  border-top: 1px solid var(--sand-border);
  margin: 0;
}

/* ── FOOTER ── */
.home-footer {
  position: relative;
  z-index: 1;
  padding: 3rem 4rem;
  max-width: 1100px;
  margin: 0 auto;
  border-top: 1px solid var(--sand-border);
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 1rem;
  font-family: -apple-system, 'Helvetica Neue', sans-serif;
  font-size: 0.85rem;
  color: var(--sand-text-dim);
}

.home-footer a {
  color: var(--sand-text-dim);
  text-decoration: none;
  min-height: 44px;
  display: inline-flex;
  align-items: center;
  transition: color 0.2s;
}

@media (hover: hover) {
  .home-footer a:hover { color: var(--sand-text); }
}

.footer-links { display: flex; gap: 1.5rem; }

/* ── SCROLL REVEAL ── */
.reveal {
  opacity: 0;
  transform: translateY(24px);
  transition: opacity 0.7s ease, transform 0.7s ease;
}

.reveal.visible {
  opacity: 1;
  transform: translateY(0);
}

/* ══════════════════════════
   RESPONSIVE
   ══════════════════════════ */

@media (max-width: 900px) {
  .hero-body {
    flex-direction: column;
    align-items: flex-start;
    padding: 3rem 2rem 2rem;
    gap: 2rem;
  }

  .hero-nav {
    gap: 1rem;
  }

  .section {
    padding: 4rem 2rem;
  }

  .card-grid {
    grid-template-columns: 1fr;
    gap: 1.2rem;
  }

  .texte-teaser {
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  .streaming-teaser {
    flex-direction: column;
    text-align: center;
    padding: 2rem;
  }

  .section-divider { padding: 0 2rem; }

  .home-footer {
    padding: 2.5rem 2rem;
    flex-direction: column;
    text-align: center;
  }
}

@media (max-width: 480px) {
  .hero-body { padding: 2rem 1.5rem 1.5rem; }
  .hero-nav { gap: 0.5rem; font-size: 0.9rem; }
  .section { padding: 3rem 1.5rem; }
  .streaming-teaser { padding: 1.5rem; }
  .section-divider, .home-footer { padding-left: 1.5rem; padding-right: 1.5rem; }
}

/* ── LETTER DISPERSION ── */
.letter {
  display: inline-block;
  transition: transform 0.6s cubic-bezier(0.23, 1, 0.32, 1), opacity 0.6s ease;
  will-change: transform;
}

.hero-name {
  cursor: default;
  user-select: none;
}

/* ── PARALLAX ── */
.hero-image {
  overflow: hidden;
}

.hero-image img {
  will-change: transform;
  transition: none;
}

/* ── FLOATING CONTROLS ── */
.floating-controls {
  position: fixed;
  bottom: 1.5rem;
  left: 1.5rem;
  display: flex;
  gap: 0.5rem;
  z-index: 100;
}

.ctrl-btn {
  width: 44px;
  height: 44px;
  border-radius: 50%;
  border: 1.5px solid var(--sand-border);
  background: var(--sand-surface);
  color: var(--sand-text-muted);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.1rem;
  transition: border-color 0.3s, color 0.3s;
  -webkit-tap-highlight-color: transparent;
}

@media (hover: hover) {
  .ctrl-btn:hover {
    border-color: var(--sand-accent);
    color: var(--sand-text);
  }
}

.ctrl-btn:active {
  transform: scale(0.95);
}

.ctrl-btn.active {
  border-color: var(--sand-accent);
  color: var(--sand-accent);
}

/* ── CARD TILT ── */
@media (hover: hover) {
  .card {
    transform-style: preserve-3d;
    perspective: 800px;
  }
}

.sr-only {
  position: absolute; width: 1px; height: 1px; padding: 0;
  margin: -1px; overflow: hidden; clip: rect(0,0,0,0);
  white-space: nowrap; border: 0;
}

@media (max-width: 900px) {
  .floating-controls {
    bottom: 1rem;
    left: 1rem;
  }
}
</style>

<!-- ═══════════════════════════════ -->
<!--  HERO                           -->
<!-- ═══════════════════════════════ -->
<div class="hero-home">
  <nav class="hero-nav" role="navigation" aria-label="Hauptnavigation">
    <a href="/" class="nav-home" aria-label="Startseite">
      <img src="/images/streaming-avatar.png" alt="" class="nav-logo" width="32" height="32">
      Jochen Hornung
    </a>
    <span class="nav-sep" aria-hidden="true">/</span>
    <a href="/texte/">Texte</a>
    <span class="nav-sep" aria-hidden="true">/</span>
    <a href="/werkzeuge/">Werkzeuge</a>
    <span class="nav-sep" aria-hidden="true">/</span>
    <a href="/kaktus/">Streaming</a>
    <span class="nav-sep" aria-hidden="true">/</span>
    <a href="/ueber/">Über mich</a>
  </nav>

  <div class="hero-image">
    <img src="/images/hero-banner.png"
         alt="s0f4surf3r — Vektor-Figur im Sessel, digitale Energie"
         width="1200" height="480"
         loading="eager">
  </div>

  <div class="hero-body">
    <div class="hero-text">
      <h1 class="hero-name">Raum für Menschlichkeit und Tiefe.</h1>
    </div>
  </div>

  <div class="sr-only">
    <p>Jochen Hornung – Builder, Autor, Mensch. Berlin. Texte über das Menschsein, digitale Werkzeuge für emotionale Selbsterkenntnis.</p>
  </div>
</div>

<!-- ═══════════════════════════════ -->
<!--  WAS ICH BAUE                  -->
<!-- ═══════════════════════════════ -->
<div class="section-divider"><hr></div>

<section class="section reveal" id="bauen">
  <p class="section-label">Was ich baue</p>
  <h2 class="section-heading">Dinge die sich anfühlen sollen.</h2>
  <p class="section-text">Kein Lock-in, kein Dark Pattern, kein Chi-Chi. Code im Dienst der Menschlichkeit.</p>

  <div class="card-grid">
    <a href="/werkzeuge/" class="card">
      <span class="card-icon" aria-hidden="true">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>
      </span>
      <h3 class="card-title">J-THRUST</h3>
      <p class="card-desc">Retro-Gravitation, C64-Seele. Ein Spiel das sich spielt wie ein Gefühl.</p>
    </a>

    <a href="/werkzeuge/" class="card">
      <span class="card-icon" aria-hidden="true">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
      </span>
      <h3 class="card-title">M-Sphere</h3>
      <p class="card-desc">Meditation als Erlebnis. Eine Kugel die atmet, auf allen Apple-Geräten.</p>
    </a>

    <a href="/werkzeuge/" class="card">
      <span class="card-icon" aria-hidden="true">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
      </span>
      <h3 class="card-title">Klarsite</h3>
      <p class="card-desc">Websites die Menschen zeigen, nicht verkaufen. Für die, die mehr sind als ihre Online-Präsenz.</p>
    </a>
  </div>
</section>

<!-- ═══════════════════════════════ -->
<!--  TEXTE                          -->
<!-- ═══════════════════════════════ -->
<div class="section-divider"><hr></div>

<section class="section reveal" id="texte">
  <p class="section-label">Texte</p>
  <div class="texte-teaser">
    <blockquote class="texte-quote">
      Wir haben uns trainiert, schlechte Maschinen zu sein. Jetzt, wo echte Maschinen das besser können, bleibt die Frage — was bleibt, wenn man den Lärm weglässt?
    </blockquote>
    <div class="texte-link-wrap">
      <p>Gedichte, Essays und persönliche Texte über das Menschsein in einer Zeit, die uns zu Maschinen machen will.</p>
      <a href="/texte/" class="text-link">Alle Texte lesen</a>
    </div>
  </div>
</section>

<!-- ═══════════════════════════════ -->
<!--  STREAMING                      -->
<!-- ═══════════════════════════════ -->
<div class="section-divider"><hr></div>

<section class="section reveal" id="streaming">
  <p class="section-label">Streaming</p>
  <a href="/kaktus/" class="streaming-teaser">
    <img src="/images/streaming-avatar.png"
         alt="s0f4surfer"
         class="streaming-avatar"
         width="72" height="72"
         loading="lazy">
    <div class="streaming-info">
      <span class="streaming-badge">Coming soon</span>
      <p>Retro Game Dev, Meditation Tech und AI Experiments — live auf Twitch.</p>
    </div>
  </a>
</section>

<!-- ═══════════════════════════════ -->
<!--  FOOTER                         -->
<!-- ═══════════════════════════════ -->
<!-- Floating Controls -->
<div class="floating-controls">
  <button class="ctrl-btn" id="soundToggle" aria-label="Sound an/aus" title="Sound">&#9835;</button>
</div>

<footer class="home-footer">
  <span>© 2026 Jochen Hornung</span>
  <div class="footer-links">
    <a href="/impressum/">Impressum</a>
    <a href="/datenschutz/">Datenschutz</a>
    <a href="mailto:kontakt@jochenhornung.de">Kontakt</a>
  </div>
</footer>

<script>
(function() {
  var reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  /* ═══ SCROLL REVEAL ═══ */
  if (reducedMotion) {
    document.querySelectorAll('.reveal').forEach(function(el) { el.classList.add('visible'); });
  } else {
    var observer = new IntersectionObserver(function(entries) {
      entries.forEach(function(entry) {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.15, rootMargin: '0px 0px -60px 0px' });
    document.querySelectorAll('.reveal').forEach(function(el) { observer.observe(el); });
  }

  /* ═══ LETTER DISPERSION ═══ */
  var nameEl = document.querySelector('.hero-name');
  if (nameEl && !reducedMotion) {
    var original = nameEl.textContent;
    nameEl.innerHTML = original.split('').map(function(ch) {
      if (ch === ' ') return ' ';
      if (ch === '\n') return '<br>';
      return '<span class="letter">' + ch + '</span>';
    }).join('');

    var scattered = false;
    var returnTimer;

    function scatter() {
      var letters = nameEl.querySelectorAll('.letter');
      letters.forEach(function(l) {
        var x = (Math.random() - 0.5) * 10;
        var y = (Math.random() - 0.5) * 8;
        var r = (Math.random() - 0.5) * 6;
        l.style.transform = 'translate(' + x + 'px,' + y + 'px) rotate(' + r + 'deg)';
        l.style.opacity = (0.65 + Math.random() * 0.35).toString();
      });
      scattered = true;
    }

    function unscatter() {
      nameEl.querySelectorAll('.letter').forEach(function(l) {
        l.style.transform = '';
        l.style.opacity = '';
      });
      scattered = false;
    }

    /* Desktop: hover */
    nameEl.addEventListener('mouseenter', function() {
      clearTimeout(returnTimer);
      scatter();
    });
    nameEl.addEventListener('mouseleave', function() {
      unscatter();
    });

    /* Touch: tap to scatter, auto-return */
    nameEl.addEventListener('touchstart', function(e) {
      e.preventDefault();
      clearTimeout(returnTimer);
      if (scattered) {
        unscatter();
      } else {
        scatter();
        returnTimer = setTimeout(unscatter, 1800);
      }
    }, { passive: false });
  }

  /* ═══ BANNER PARALLAX ═══ */
  if (!reducedMotion) {
    var bannerImg = document.querySelector('.hero-image img');
    if (bannerImg) {
      var ticking = false;
      window.addEventListener('scroll', function() {
        if (!ticking) {
          requestAnimationFrame(function() {
            var scrollY = window.pageYOffset;
            var rect = bannerImg.parentElement.getBoundingClientRect();
            if (rect.bottom > 0) {
              bannerImg.style.transform = 'translateY(' + (scrollY * 0.08) + 'px)';
            }
            ticking = false;
          });
          ticking = true;
        }
      });
    }
  }

  /* ═══ CARD TILT ═══ */
  if (!reducedMotion && window.matchMedia('(hover: hover)').matches) {
    document.querySelectorAll('.card').forEach(function(card) {
      card.addEventListener('mousemove', function(e) {
        var rect = card.getBoundingClientRect();
        var x = (e.clientX - rect.left) / rect.width - 0.5;
        var y = (e.clientY - rect.top) / rect.height - 0.5;
        card.style.transform = 'translateY(-2px) rotateY(' + (x * 4) + 'deg) rotateX(' + (-y * 4) + 'deg)';
      });
      card.addEventListener('mouseleave', function() {
        card.style.transform = '';
      });
    });
  }

  /* ═══ AMBIENT SOUND ═══ */
  var audioCtx, gainNode, isPlaying = false;
  var soundBtn = document.getElementById('soundToggle');

  function createAmbient() {
    audioCtx = new (window.AudioContext || window.webkitAudioContext)();

    /* Two detuned sine waves — barely audible warmth */
    var osc1 = audioCtx.createOscillator();
    var osc2 = audioCtx.createOscillator();
    var osc3 = audioCtx.createOscillator();
    osc1.type = 'sine'; osc1.frequency.value = 85;
    osc2.type = 'sine'; osc2.frequency.value = 87.5;
    osc3.type = 'triangle'; osc3.frequency.value = 170;

    var filter = audioCtx.createBiquadFilter();
    filter.type = 'lowpass';
    filter.frequency.value = 300;
    filter.Q.value = 1;

    gainNode = audioCtx.createGain();
    gainNode.gain.value = 0;

    osc1.connect(filter);
    osc2.connect(filter);
    osc3.connect(filter);
    filter.connect(gainNode);
    gainNode.connect(audioCtx.destination);

    osc1.start();
    osc2.start();
    osc3.start();
  }

  if (soundBtn) {
    soundBtn.addEventListener('click', function() {
      if (!audioCtx) createAmbient();

      if (audioCtx.state === 'suspended') {
        audioCtx.resume();
      }

      if (isPlaying) {
        gainNode.gain.linearRampToValueAtTime(0, audioCtx.currentTime + 0.8);
        isPlaying = false;
        soundBtn.classList.remove('active');
      } else {
        gainNode.gain.linearRampToValueAtTime(0.025, audioCtx.currentTime + 1.5);
        isPlaying = true;
        soundBtn.classList.add('active');
      }
    });
  }
})();
</script>
