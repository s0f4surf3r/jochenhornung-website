---
layout: base.njk
title: Raum für Menschlichkeit und Tiefe
---

<style>
    /* Spezielle Regeln für die Startseite */
    html, body {
        margin: 0;
        padding: 0;
        background: #000; /* Schwarz verhindert graue Blitzer beim Laden */
        height: 100%;
        overflow-x: hidden;
    }

    .hero {
        background-image: url('/images/hero.jpg');
        background-size: cover;
        background-position: center center;
        /* dvh füllt den sichtbaren Bereich perfekt aus, ohne zu scrollen */
        height: 100dvh;
        width: 100%;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        align-items: center;
        position: relative;
        /* Nutzt Safe Area Abstände für Notch und Home-Indicator */
        padding: calc(2rem + env(safe-area-inset-top)) 1.5rem calc(2rem + env(safe-area-inset-bottom));
        box-sizing: border-box;
    }

    @media (max-width: 768px) {
        .hero {
            background-position: 65% center;
        }
    }

    .hero-overlay {
        position: absolute;
        inset: 0;
        background: rgba(0,0,0,0.6);
        z-index: 0;
    }

    .hero-content {
        position: relative;
        z-index: 1;
        text-align: center;
        flex: 1;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .hero-name {
        font-size: 1.5rem;
        color: white;
        margin-bottom: 1rem;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
    }

    .hero-title {
        font-size: clamp(1.8rem, 6vw, 2.5rem);
        font-weight: 700;
        color: white;
        letter-spacing: 0.08em;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
        text-transform: uppercase;
    }

    .hero-nav {
        position: relative;
        z-index: 1;
        display: flex;
        gap: 1.5rem;
        flex-wrap: wrap;
        justify-content: center;
        margin-bottom: 2rem;
    }

    .hero-nav a {
        color: white;
        text-decoration: none;
        font-size: 1.1rem;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.8);
        border-bottom: 1px solid rgba(255,255,255,0.3);
    }

    /* Sorgt dafür, dass der Footer erst beim Scrollen erscheint */
    .site-footer {
        background: #ffffff;
        position: relative;
        z-index: 2;
        border-top: none;
    }
</style>

<div class="hero">
    <div class="hero-overlay"></div>

    <div class="hero-content">
        <p class="hero-name">Jochen Hornung</p>
        <h1 class="hero-title">RAUM FÜR MENSCHLICHKEIT UND TIEFE</h1>
    </div>

    <nav class="hero-nav">
        <a href="/texte/">Texte</a>
        <a href="/coaching/">Coaching</a>
        <a href="/werkzeuge/">Werkzeuge</a>
        <a href="/kaktus/">Kaktus</a>
        <a href="/ueber/">Über mich</a>
    </nav>
</div>
