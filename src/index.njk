---
layout: base.njk
title: Raum für Menschlichkeit und Tiefe
---

<style>
    .hero {
        background-image: url('/images/hero.jpg');
        background-size: cover;
        background-position: center center;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        align-items: center;
        padding: 4rem 1.5rem;
        position: relative;
    }

    /* Auf schmalen Screens: Kaktus rechts sichtbar + mehr Platz unten */
    @media (max-width: 768px) {
        .hero {
            background-position: 65% center;
            padding-bottom: 8rem;
        }
    }

    .hero-overlay {
        position: absolute;
        inset: 0;
        background: rgba(0,0,0,0.6);
    }

    .hero-content {
        position: relative;
        z-index: 1;
        text-align: center;
        flex: 1;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .hero-name {
        font-size: 1.5rem;
        font-weight: 400;
        color: white;
        margin-bottom: 1rem;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
    }

    .hero-title {
        font-size: clamp(2.1rem, 6vw, 2.5rem);
        font-weight: 700;
        color: white;
        letter-spacing: 0.08em;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
    }

    .hero-nav {
        position: relative;
        z-index: 1;
        display: flex;
        row-gap: 0.8rem;
        column-gap: 2rem;
        flex-wrap: wrap;
        justify-content: center;
    }

    .hero-nav a {
        color: white;
        text-decoration: none;
        font-size: 1.1rem;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.8);
    }
</style>

<div class="hero">
    <div class="hero-overlay"></div>

    <div class="hero-content">
        <p class="hero-name">Jochen Hornung</p>
        <h1 class="hero-title">RAUM FÜR MENSCHLICHKEIT UND TIEFE</h1>
    </div>

    <nav class="hero-nav">
        <a href="/texte/">Texte</a>
        <a href="/coaching/">Coaching</a>
        <a href="/werkzeuge/">Werkzeuge</a>
        <a href="/kaktus/">Kaktus</a>
        <a href="/ueber/">Über mich</a>
    </nav>
</div>
