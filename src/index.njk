---
layout: base.njk
title: Raum für Menschlichkeit und Tiefe
---

<style>
    /* Startseite: dunkler Hintergrund (damit oben/unten nie weiß durchscheint) */
    html, body {
        background: #1a1a1a;
    }

    .hero {
        background-image: url('/images/hero.jpg');
        background-size: cover;
        background-position: center center;

        /*
          Overscan:
          - 100svh = sichtbarer Viewport (stabiler als vh)
          - + safe-area-top = Notch-Zone
          - + 24px = kleiner Puffer für Safari UI/Render-Zickigkeit
          Das ist die "Hero länger"-Variante ohne wildes vh-Geschwurbel.
        */
        min-height: calc(100svh + env(safe-area-inset-top) + 24px);

        /* Fallbacks */
        min-height: calc(100vh + env(safe-area-inset-top) + 24px);

        display: flex;
        flex-direction: column;
        justify-content: space-between;
        align-items: center;

        /* Padding bleibt wie bei dir, safe-area sauber drin */
        padding:
            calc(4rem + env(safe-area-inset-top))
            1.5rem
            calc(2.5rem + env(safe-area-inset-bottom));

        position: relative;

        /* Schutz gegen Mini-Glitches/1px lines */
        overflow: hidden;
    }

    @media (max-width: 768px) {
        .hero {
            background-position: 65% center;
        }
    }

    .hero-overlay {
        position: absolute;
        inset: 0;
        background: rgba(0,0,0,0.6);
    }

    .hero-content {
        position: relative;
        z-index: 1;
        text-align: center;
        flex: 1;
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .hero-name {
        font-size: 1.5rem;
        font-weight: 400;
        color: white;
        margin-bottom: 1rem;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
    }

    .hero-title {
        font-size: clamp(2.1rem, 6vw, 2.5rem);
        font-weight: 700;
        color: white;
        letter-spacing: 0.08em;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
    }

    .hero-nav {
        position: relative;
        z-index: 1;
        display: flex;
        row-gap: 0.8rem;
        column-gap: 2rem;
        flex-wrap: wrap;
        justify-content: center;

        /* WICHTIG: keine großen margins nach unten, sonst wieder Scroll-Random */
        margin: 0;
    }

    @media (max-width: 768px) {
        .hero-nav {
            max-width: 340px;
        }
    }

    .hero-nav a {
        color: white;
        text-decoration: none;
        font-size: 1.1rem;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.8);
    }
</style>

<div class="hero">
    <div class="hero-overlay"></div>

    <div class="hero-content">
        <p class="hero-name">Jochen Hornung</p>
        <h1 class="hero-title">RAUM FÜR MENSCHLICHKEIT UND TIEFE</h1>
    </div>

    <nav class="hero-nav">
        <a href="/texte/">Texte</a>
        <a href="/coaching/">Coaching</a>
        <a href="/werkzeuge/">Werkzeuge</a>
        <a href="/kaktus/">Kaktus</a>
        <a href="/ueber/">Über mich</a>
    </nav>
</div>